## 1. 项目基础

- [x] 1.1 确认 v3 实现使用的前端框架、数据库、实时同步方案和服务端运行环境。
- [x] 1.2 如果当前没有前端脚手架，创建移动端优先的简体中文应用基础结构。
- [x] 1.3 增加共享领域类型：玩家身份、游戏回合、问答记录、已猜出历史项、AI 判定值。
- [x] 1.4 定义 AI 判定枚举，取值严格为 `是`、`不是`、`不确定`、`无关`、`猜对了`。

## 2. 持久化层

- [x] 2.1 创建数据库表或集合：游戏回合、问答记录、已猜出人物历史。
- [x] 2.2 增加索引和约束：同一时间只有一个未完成当前回合、同回合问答序号唯一、已猜出人物可检查重复。
- [x] 2.3 实现数据读取函数：主页汇总计数、当前回合、当前时间线、已猜出列表、历史问答记录。
- [x] 2.4 实现刷新恢复，所有可见游戏状态都能从持久化记录重建。

## 3. AI 与服务端动作

- [x] 3.1 实现 AI 判定提示词，根据隐藏人物判断玩家问题。
- [x] 3.2 实现 AI 回复规范化和兜底处理，确保只有五个允许判定值会被保存。
- [x] 3.3 实现 AI 生成人物，并把所有已猜出人物名称加入避免重复的上下文。
- [x] 3.4 实现重复人物拒绝逻辑，生成结果重复时不能激活新回合。
- [x] 3.5 实现提交问题的服务端动作：校验输入、串行分配序号、调用 AI、保存完整问答条目，并在 `猜对了` 时标记回合完成。
- [x] 3.6 实现 `再猜一个` 的幂等下一回合创建逻辑。
- [x] 3.7 识别玩家明确“直接揭晓答案”的请求，并在揭晓后立即开启新的当前回合。
- [x] 3.8 强化 AI 判定提示词和兜底逻辑，使朝代、身份等相关问题不再轻易误判为 `无关`。
- [x] 3.9 识别玩家的提示请求，生成不泄露答案且尽量不重复的人物线索，并持久化为独立回复类型。

## 4. 实时同步

- [x] 4.1 增加当前回合、完整问答条目、已猜出历史的实时订阅，或等价的轮询兜底。
- [x] 4.2 确保问答条目只有在 AI 判定持久化成功后，才同步给所有在线玩家。
- [x] 4.3 确保当前回合被猜对后，所有客户端都会触发猜对结果弹窗。
- [x] 4.4 处理重连场景：重新拉取持久化状态，并补齐错过的实时更新。

## 5. 入场身份 UI

- [x] 5.1 构建首次访问入场弹窗，提供至少 20 个预设 emoji 头像。
- [x] 5.2 增加昵称校验：2 到 8 个字符，并要求选择头像后才能确认。
- [x] 5.3 将确认后的 emoji 头像和昵称保存在本地，并在后续访问中复用。
- [x] 5.4 玩家每次提交问题时，附带本地保存的 emoji 头像和昵称。

## 6. 主页大厅 UI

- [x] 6.1 构建主页顶部状态栏，展示累计猜对数量和累计提问数量。
- [x] 6.2 构建大型中央 `?` 谜题卡片，并展示 `已提问 N 次`。
- [x] 6.3 让整张谜题卡片都可以点击，并导航到问答页。
- [x] 6.4 构建底部已猜出人物列表，展示人物名称和提问次数。
- [x] 6.5 构建没有已猜出人物时的空状态。
- [x] 6.6 构建历史问答弹窗，包含人物标题、完整时间线和 `关闭` 按钮。

## 7. 问答页 UI

- [x] 7.1 构建问答页顶部栏，包含左侧返回按钮和页面标题。
- [x] 7.2 构建竖向时间线布局，包含序号、问题标题、右对齐提问者身份、左对齐 AI 判定。
- [x] 7.3 按序号从小到大排序时间线，让旧问题在上、新问题在下。
- [x] 7.4 为五种 AI 判定增加不同的文字颜色。
- [x] 7.5 构建底部输入区，包含占位提示 `是某个朝代/身份/性别/功绩...吗？` 和发送按钮。
- [x] 7.6 玩家发送后显示 `AI 判定中...`，并在服务端动作完成前阻止该玩家重复发送。
- [x] 7.7 只有持久化成功后，才把问题作为完整的“问题 + 判定”条目渲染到时间线。
- [x] 7.8 在时间线中以独立样式展示 AI 人物提示，并在刷新后恢复。

## 8. 猜对结果流程

- [x] 8.1 构建猜对结果弹窗：猜对者看到 `你猜对了！`，其他人看到 `[昵称] 猜对了！答案是 [人物名]！`。
- [x] 8.2 将 `再猜一个` 连接到关闭弹窗并激活或加入下一当前回合。
- [x] 8.3 将 `返回主页` 连接到关闭弹窗并导航回主页。
- [x] 8.4 确保回合猜对后，主页汇总和已猜出历史会更新。

## 9. 样式与本地化

- [x] 9.1 为入场弹窗、主页、问答页、结果弹窗、历史弹窗应用浅黄色古风视觉主题。
- [x] 9.2 确保所有 UI 文案使用简体中文。
- [x] 9.3 增加宣纸底色、墨色文字、朱砂点缀、印章式按钮、卷轴式弹窗和竹简式时间线等古风元素。
- [ ] 9.4 验证古风纹理和装饰不会影响文字可读性、点击区域和移动端滚动体验。
- [ ] 9.5 验证移动端优先布局不会横向滚动，并保持界面简洁、卡片密度较低。

## 10. 验证

- [ ] 10.1 测试首次访问、身份确认、带身份刷新、无效昵称拦截。
- [ ] 10.2 测试主页计数、谜题卡片跳转、已猜出人物列表、历史问答弹窗。
- [ ] 10.3 测试问题提交等待态、AI 判定规范化、时间线排序、完整条目渲染。
- [ ] 10.4 测试两个客户端之间的问答同步和猜对结果弹窗广播。
- [x] 10.5 测试主页和问答页刷新后的状态恢复。
- [x] 10.6 测试多个客户端同时点击 `再猜一个` 时不会创建重复下一回合。
- [x] 10.7 测试已猜出历史存在时，新人物生成能避免重复。
- [x] 10.8 测试“是清朝吗？”这类相关问题不会误判为 `无关`，以及直接请求答案时会揭晓并开启新回合。
- [x] 10.9 测试提示请求会返回不包含答案的人物线索，连续提示尽量不同且刷新后仍存在。
